

#include<iostream>
#include<algorithm>
#include<vector>
using namespace std;
int main(){

    vector<int>v;
    v.push_back(3);
    v.push_back(8);
    v.push_back(99);
    v.push_back(2);
    v.push_back(1);

cout<<"binary serach : "<<binary_search(v.begin(),v.end(),8);
}
